cmake_minimum_required(VERSION 2.8)
project(LogisticRegression)
find_package(OpenCV REQUIRED)
add_executable(LogisticRegression logistic_regression.cpp)
target_link_libraries(LogisticRegression ${OpenCV_LIBS})
